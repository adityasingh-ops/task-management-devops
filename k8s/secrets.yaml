apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: task-management
type: Opaque
stringData:
  jwt-secret: "your-super-secret-jwt-key-change-this-in-production"
  # In production, use: kubectl create secret generic app-secrets --from-literal=jwt-secret=<your-secret>
